"""Agents package — core agent functionality.

Includes model selection, compaction, fallback, system prompt construction,
identity management, workspace bootstrap, context window management,
provider capabilities, API key rotation, sandbox paths, cache tracing,
and sub-agent management.
"""
