"""Plugin SDK package — ported from bk/src/plugin-sdk/.

Unified API surface for plugin developers: re-exports core types + utilities
for webhooks, OAuth, file locks, keyed queues, SSRF guards, channel config helpers,
command auth, inbound/outbound media, status helpers, text chunking, etc.
"""
